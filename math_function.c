#include<stdio.h>
#include<math.h>
int main()

{

    //int x = 2;
    //x= sqrt(x); // Calculate the square root of x
    //printf("The square root of 9 is: %d\n", x); 

    // x = pow(x, 2); // Calculate x raised to the power of 3
    // printf("The value of x raised to the power of 3 is: %d\n", x);


    //round function round(x) 

    int x = -3;
    int y;
    y = abs(x); // Calculate the absolute value of x
    printf("The absolute value of -3 is: %d\n", y);


    return 0;
}