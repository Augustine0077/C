#include<stdio.h>
int main()
{
    int a, b, sum;
    // This program calculates the sum of two integers
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("The sum is: %d\n", sum);
    return 0;
}

